// Fonction d'addition
function addition(a, b) {
  return a + b;
}

// Fonction de multiplication (optionnelle pour complexité)
function multiplication(a, b) {
  return a * b;
}

// Export des fonctions
module.exports = {
  addition,
  multiplication
};